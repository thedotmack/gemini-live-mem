# Anthropic API key for the Claude tool-use loop (model claude-sonnet-4-6).
# Leave this BLANK to run the backend in DEMO MODE: the agent will run a
# deterministic canned script (navigate -> screenshot -> narrate) and stream
# valid AG-UI SSE without calling Claude and without launching chromium.
# Set a real key to drive a live browser via Playwright + Claude.
ANTHROPIC_API_KEY=
